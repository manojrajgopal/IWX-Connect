services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: iwx
      POSTGRES_PASSWORD: iwx
      POSTGRES_DB: iwx_connect
    ports: ["5432:5432"]
    volumes: ["pgdata:/var/lib/postgresql/data"]
volumes:
  pgdata: {}
